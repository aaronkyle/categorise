import {defineConfig} from "@observablehq/framework";

export default defineConfig({
  title: "My Site",
  root: "src",
  style: "styles/categori-se.css", // <— global CSS
  // theme: ["air","wide"], // optional
});
